// swift-tools-version:4.0
import PackageDescription

let package = Package(
    name: "ResourceQueue",
    products: [
        .library(
            name: "ResourceQueue",
            targets: ["ResourceQueue"]),
    ],
    dependencies: [
        
    ],
    targets: [
        .target(
            name: "ResourceQueue",
            dependencies: [  ])
    ]
)
